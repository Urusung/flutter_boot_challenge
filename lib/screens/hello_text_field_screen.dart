import 'package:flutter/material.dart';

class HelloTextFieldScreen extends StatelessWidget {
  const HelloTextFieldScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
