import 'package:flutter/material.dart';

class HelloLayoutScreen extends StatelessWidget {
  const HelloLayoutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
